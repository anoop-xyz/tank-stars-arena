services:
  - type: web
    name: tank-stars-arena-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20.14.0
      - key: CLIENT_ORIGIN
        value: "*"
